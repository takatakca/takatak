/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    trailingSlash: true,
    // assetPrefix: '/takatak/',
    // images: { unoptimized: true }
};

export default nextConfig;

